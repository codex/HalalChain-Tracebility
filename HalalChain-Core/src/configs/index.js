const F_FAN_CONFIG = FFAN_CONFIG;
const configs = {
  secureHttps: false,
  host: F_FAN_CONFIG.host,
  port: F_FAN_CONFIG.port,
  apiRoot: F_FAN_CONFIG.apiRoot,
  virtualPath: F_FAN_CONFIG.virtualPath,
  authToken: F_FAN_CONFIG.authToken,
  storage: F_FAN_CONFIG.storage,
  staticPrefix: F_FAN_CONFIG.staticPrefix,
  storageNameSpace: F_FAN_CONFIG.storageNameSpace
};
export default configs;
